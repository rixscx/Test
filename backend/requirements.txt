# ==============================================================================
# 1. CORE WEB FRAMEWORK (MANDATORY for Django & REST API)
# ==============================================================================
Django==3.2.25
djangorestframework==3.14.0
django-cors-headers==3.13.0
python-dotenv==0.21.1

# ==============================================================================
# 2. USER AUTHENTICATION (ALLAUTH FIX)
# ==============================================================================
django-allauth==0.61.1       
requests==2.28.2
requests-oauthlib


# ==============================================================================
# 3. DEEP LEARNING / AI INFERENCE (Required for 'analyzer' and 'chat' apps)
# ==============================================================================
# If you need the 'chat' (LLM/RAG) feature:
huggingface-hub

# Core ML/DL
torch==2.2.0          # PyTorch backend, required for model_e220_v-4.700.pth
Pillow==10.1.0        # Image processing (required for image inputs to 'analyzer')

# Hugging Face ecosystem (likely used in the 'chat' app)
transformers==4.35.2
torchvision==0.17.0   # Often paired with torch for vision tasks

# General Data Science/ML Utilities
numpy==1.26.2
pandas==2.1.3
scikit-learn==1.3.2
openai

# ==============================================================================
# 4. OPTIONAL ADVANCED ML (Fine-Tuning/Acceleration)
# ------------------------------------------------------------------------------
# **Only uncomment these if your 'chat' or 'analyzer' apps use advanced**
# **features like model fine-tuning or quantization.**
# ==============================================================================
# peft==0.6.2           # Parameter-Efficient Fine-Tuning
# accelerate==0.25.0    # Distributed training/inference support